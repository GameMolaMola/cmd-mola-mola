import type { LevelConfig } from '../types';

export const level3Config: LevelConfig = {
  enemyCount: 6,
  coinCount: 11,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 2,
};

export default level3Config;
