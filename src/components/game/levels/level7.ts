import type { LevelConfig } from '../types';

export const level7Config: LevelConfig = {
  enemyCount: 10,
  coinCount: 19,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 3,
};

export default level7Config;
