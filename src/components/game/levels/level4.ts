import type { LevelConfig } from '../types';

export const level4Config: LevelConfig = {
  enemyCount: 7,
  coinCount: 13,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 2,
};

export default level4Config;
