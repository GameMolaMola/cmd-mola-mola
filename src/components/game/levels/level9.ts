import type { LevelConfig } from '../types';

export const level9Config: LevelConfig = {
  enemyCount: 12,
  coinCount: 23,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 4,
};

export default level9Config;
