import type { LevelConfig } from '../types';

export const level5Config: LevelConfig = {
  enemyCount: 8,
  coinCount: 15,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 2,
  background: 'jungle',
};

export default level5Config;
