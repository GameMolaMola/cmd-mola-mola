import type { LevelConfig } from '../types';

export const level1Config: LevelConfig = {
  enemyCount: 4,
  coinCount: 7,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 0,
};

export default level1Config;
