import type { LevelConfig } from '../types';

export const level8Config: LevelConfig = {
  enemyCount: 11,
  coinCount: 21,
  pizzaCount: 3,
  brasilenaCount: 2,
  wineCount: 2,
  swordfishCount: 3,
  background: 'jungle',
};

export default level8Config;
